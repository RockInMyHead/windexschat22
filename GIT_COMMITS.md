# Git Commits Guide

## 🚀 Быстрый старт

### Автоматический коммит
```bash
npm run commit
```
или
```bash
./auto-commit.sh
```

### Свой коммит с сообщением
```bash
./auto-commit.sh "Добавил новую функцию X"
```

### Автоматические коммиты каждые 5 минут
```bash
npm run commit:watch
```
или
```bash
./commit-watch.sh
```

## 📋 Доступные скрипты

### `npm run commit`
- Автоматически добавляет все изменения
- Создает описательное сообщение коммита
- Пушит на remote (если настроен)

### `npm run commit:watch`
- Запускает watcher, который делает коммиты каждые 5 минут
- Полезно во время активной разработки

### `./auto-commit.sh [message]`
- То же самое, что и `npm run commit`
- Можно передать собственное сообщение коммита

### `./commit-watch.sh`
- То же самое, что и `npm run commit:watch`

## 🔧 Настройка

### .gitignore
Проект уже настроен с полным `.gitignore`, который игнорирует:
- `node_modules/`
- `dist/`
- Базы данных (`.db`, `.sqlite`)
- Логи (`.log`)
- Переменные окружения (`.env*`)
- Куки и сессии
- Архивы (`.tar.gz`, `.zip`)
- Временные файлы

### Remote репозиторий
Если хотите пушить на GitHub/GitLab:
```bash
git remote add origin https://github.com/your-username/your-repo.git
git push -u origin main
```

## 📊 Формат коммитов

Автоматические коммиты используют формат:
```
🔄 Auto-commit: X files changed (+Y new) (Z modified) - YYYY-MM-DD HH:MM:SS
```

## 🛠 Ручное управление

### Статус репозитория
```bash
git status
```

### Просмотр истории
```bash
git log --oneline
```

### Отмена последнего коммита (сохраняя изменения)
```bash
git reset --soft HEAD~1
```

### Создание новой ветки
```bash
git checkout -b feature/new-feature
```

## ⚡ Советы

1. **Регулярные коммиты**: Делайте коммиты часто, чтобы не потерять работу
2. **Описательные сообщения**: Для важных изменений пишите понятные сообщения
3. **Ветки**: Используйте ветки для новых функций
4. **Pull перед push**: Всегда синхронизируйтесь с remote

## 🔒 Безопасность

- Переменные окружения (`.env`) игнорируются git
- Чувствительные данные не должны попадать в репозиторий
- Используйте `.gitignore` для защиты конфиденциальных файлов