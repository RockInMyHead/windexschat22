# 🆘 Экстренная очистка сервера

## ⚠️ Проблема
Диск сервера 176.109.111.72 заполнен на **100%**. Необходима срочная очистка.

---

## 🔧 Решение (выполните эти команды на сервере)

### 1. Подключитесь к серверу
```bash
ssh user1@176.109.111.72
```

---

### 2. Проверьте текущую ситуацию
```bash
df -h
du -sh /home/user1/* 2>/dev/null | sort -rh
```

---

### 3. Экстренная очистка (выполняйте по порядку)

#### A. Удалите старые логи
```bash
sudo rm -f /var/log/*.log.* /var/log/*.gz
sudo rm -f /var/log/*/*.log.* /var/log/*/*.gz
sudo journalctl --vacuum-size=50M
```

#### B. Очистите apt cache
```bash
sudo apt-get clean
sudo apt-get autoclean
sudo apt-get autoremove -y
```

#### C. Очистите временные файлы
```bash
sudo rm -rf /tmp/*
sudo rm -rf /var/tmp/*
```

#### D. Удалите старые npm логи
```bash
rm -rf ~/.npm/_logs/*
```

#### E. Найдите и удалите большие файлы
```bash
# Найти файлы больше 100MB в /home/user1
find /home/user1 -type f -size +100M -exec ls -lh {} \;

# Если найдете ненужные, удалите их:
# rm -f /path/to/large/file
```

#### F. Удалите старые node_modules (если есть старые проекты)
```bash
# Найти все node_modules
find /home/user1 -type d -name "node_modules" -exec du -sh {} \;

# Удалите ненужные (осторожно!)
# rm -rf /path/to/old/project/node_modules
```

#### G. Очистите Docker (если установлен)
```bash
docker system prune -af 2>/dev/null || echo "Docker не установлен"
```

---

### 4. Проверьте освобожденное место
```bash
df -h
```

Вам нужно освободить **минимум 2-3 GB** для успешного деплоя.

---

### 5. После очистки - повторите деплой

С вашей локальной машины:
```bash
./deploy-to-server.sh
```

---

## 🎯 Быстрый вариант (одна команда)

Если вы доверяете автоматической очистке, выполните на сервере:

```bash
sudo bash -c '
  echo "🧹 Экстренная очистка..."
  apt-get clean
  apt-get autoclean
  apt-get autoremove -y
  rm -rf /tmp/*
  rm -rf /var/tmp/*
  rm -f /var/log/*.log.* /var/log/*.gz /var/log/*/*.log.* /var/log/*/*.gz
  journalctl --vacuum-size=50M
  find /home/user1/.npm -type f -name "*.log" -delete 2>/dev/null
  echo "✅ Готово!"
  df -h
'
```

---

## 📊 Дополнительная диагностика

### Найти самые большие директории
```bash
du -h --max-depth=1 /home/user1 | sort -rh | head -n 20
```

### Найти самые большие файлы
```bash
find /home/user1 -type f -exec du -h {} + 2>/dev/null | sort -rh | head -n 20
```

### Проверить использование по типам файлов
```bash
find /home/user1 -type f -name "*.log" -exec du -ch {} + | tail -1
find /home/user1 -type f -name "*.tar.gz" -exec du -ch {} + | tail -1
```

---

## 🚨 Если ничего не помогает

### Вариант 1: Увеличьте размер диска
Обратитесь к хостинг-провайдеру для увеличения дискового пространства.

### Вариант 2: Перенесите данные
Перенесите большие файлы/папки на другой диск или в облачное хранилище.

### Вариант 3: Используйте отдельный диск для приложения
Подключите дополнительный диск и перенесите приложение туда.

---

## ✅ После успешной очистки

1. Проверьте, что освободилось минимум 2-3 GB
2. Запустите деплой с локальной машины: `./deploy-to-server.sh`
3. Настройте автоматическую очистку (см. ниже)

---

## 🔄 Автоматическая очистка (настройте после деплоя)

Добавьте в crontab на сервере:

```bash
# Открыть crontab
crontab -e

# Добавить задачи:
# Очистка логов каждую неделю (воскресенье в 3:00)
0 3 * * 0 find /var/log -type f -name "*.gz" -mtime +7 -delete

# Очистка tmp каждый день (в 4:00)
0 4 * * * rm -rf /tmp/* /var/tmp/*

# Очистка npm cache каждый месяц (1 числа в 5:00)
0 5 1 * * npm cache clean --force

# Очистка журналов systemd (оставить последние 7 дней)
0 6 * * 0 journalctl --vacuum-time=7d
```

---

**Важно:** После очистки у вас должно быть минимум **2-3 GB** свободного места для успешной установки зависимостей.
