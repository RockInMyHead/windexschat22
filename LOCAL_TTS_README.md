# 🔊 Локальный TTS для WindexsAI Chat

## 🚀 Быстрый запуск

```bash
# Запуск локального TTS сервера
./start_local_tts.sh

# Или вручную:
cd local/local/stt-vosk/backend
python3 simple_tts_server.py
```

## 🎯 Использование в чате

### Локальный TTS (@local)
- Добавьте `@local` к любому сообщению
- Нажмите кнопку озвучки (🔊)
- Речь будет сгенерирована локально без интернета

**Примеры:**
```
Привет, мир! @local
Расскажи о себе @local
```

### Облачный TTS (по умолчанию)
- Обычные сообщения без `@local`
- Используется OpenAI TTS API
- Требуется интернет и API ключ

## 🛠️ Технические детали

### Сервер
- **Порт:** 5001
- **Адрес:** http://127.0.0.1:5001
- **Метод:** Системный TTS (macOS `say`)

### API Endpoints
- `GET /health` - Проверка работоспособности
- `POST /tts` - Генерация речи

### Формат запроса
```json
{
  "text": "Текст для озвучки"
}
```

### Формат ответа
```json
{
  "success": true,
  "audio_base64": "base64_encoded_audio",
  "format": "aiff",
  "size": 123456
}
```

## 🔧 Управление сервером

### Запуск
```bash
./start_local_tts.sh
```

### Остановка
```bash
# Найти PID
cat local/tts-backend.pid
# Остановить
kill -9 <PID>
```

### Логи
```
tail -f local/logs/tts-backend.log
```

## 🎵 Особенности

- **Без зависимостей:** Использует системный TTS
- **Локальная обработка:** Не требует интернета
- **Быстрая генерация:** Мгновенный отклик
- **MacOS only:** Работает только на macOS с командой `say`

## 🔍 Диагностика

### Проверка работы
```bash
# Health check
curl http://127.0.0.1:5001/health

# Тест генерации
curl -X POST -H "Content-Type: application/json" \
  -d '{"text":"Тест"}' http://127.0.0.1:5001/tts
```

### Возможные проблемы
- **Порт занят:** Убейте процесс на порту 5001
- **Нет звука:** Проверьте системные настройки звука
- **Сервер не запускается:** Проверьте логи

## 💡 Советы

1. **Автозапуск:** Добавьте `./start_local_tts.sh` в автозагрузку
2. **Тестирование:** Используйте короткие сообщения для тестов
3. **Мониторинг:** Следите за логами на ошибки

---

**Готово! 🎉 Теперь вы можете использовать локальный TTS в WindexsAI Chat.**